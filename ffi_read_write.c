#include "ffi_read_write.h"
#include "ffi_dstru.h"

#include <stdio.h>

#define DEBUG

int ffi_read(struct dstru_struct *storage, const char *query);
int ffi_write();

int ffi_read(struct dstru_struct *storage, const char *location){

    return 0;
}

int ffi_write(struct dstru_struct *storage, const char *location){

    return 0;
}

