#include "ffi_read_write.h"
#include "ffi_dstru.h"

#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

#define DEBUG


int ffi_parse_location(struct query_op **arr, const char *location);

int ffi_parse_location(struct query_op **arr, const char *location){


    return 0;
}

int ffi_read(struct dstru_struct *storage, const char *query, char **result);
int ffi_write();

int ffi_read(struct dstru_struct *storage, const char *location, char **result){
    ffilocset_in

    return 0;
}

int ffi_write(struct dstru_struct *storage, const char *location){

    return 0;
}

